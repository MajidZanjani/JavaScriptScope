let warrior1 = 3;

const screamWarriors = () => {
  let warrior2 = 5;

  return `Our warriors are warrior${warrior1} and warrior${warrior2}. Multiply their numbers will be ${
    warrior1 * warrior2
  }.`;
};

console.log(screamWarriors());
